# Original User Request

## 2026-09-16T13:15:35Z

# Teamwork Project Prompt — Draft

> Status: Launched
> Goal: 深度审计差距 → 制定精准补全任务清单 → 已委派 teamwork_preview 执行中
> Requested team: Full team (全功能研发审计与补全团队)

项目目标：将 Open Generative AI (KoyoSIM AI Studio) 补全为具备独立商业发布、可稳定交付 C 端真实用户使用的生产就绪系统。严禁增补无关多余功能，专注打通“前端工作台生成-额度计费-异步Worker-作品资产沉淀-支付充值-登录与合规”全链路闭环。

Working directory: h:\03 Dev  Lake
Integrity mode: development

## Requirements

### R1. 前端 Studio 工作台与后端统一生成/计费引擎闭环
前端全部创作工作室（图片生成、视频生成、分层拆解、AI 影院、工作流等）必须废除绕过计费的直接上游代理模式，统一接入服务端 `/api/generations` 契约：
- 提交任务时执行服务端身份校验、防刷限流与 PostgreSQL 原子额度预扣；
- 支持长任务异步轮询与状态追踪，任务状态变更与计费扣除/释放严格保持事务一致；
- 生成成功后，生成结果自动沉淀至用户“个人作品资产库”（`/creations`），支持一键分享至社区（`/community`）与做同款（Remix）；
- 用户额度不足时，前端提供清晰友好的升级/充值引导，严禁裸报 500 或无响应。

### R2. 异步 Worker 生产常驻守护与任务超时自愈 (Watchdog)
将耗时的大模型生成任务从 Web HTTP 线程中完全剥离，由独立后台 Worker 守护进程消费：
- 建立生产级进程守护（Systemd / PM2），实现异常退出自动重启与平滑退出连接池释放；
- 支持服务器重启后的未完成任务恢复消费；
- 建立 Watchdog 超时熔断机制：针对因网络闪断、三方崩溃超过限定时长的任务，自动释放预扣额度并标记为可审计的超时失败状态，保障用户资产不受损。

### R3. PostgreSQL 16 数据库迁移补齐与独立测试验收
彻底解决生产数据库迁移滞后与测试环境缺失问题：
- 在云端配置独立的测试数据库 `TEST_DATABASE_URL`，跑通全量 `npm run test:db`（6 passed / 0 skipped）与迁移/备份演练；
- 在完整备份前提下，将线上数据库从 `006_community_and_social` 安全推进并完成 `007` 至 `013` 全部迁移，对齐任务状态机、模型目录、安全门禁、支付唯一防重与退款流水表结构；
- 消除迁移文件同名编号隐患，验证迁移回滚与 Checksum 一致性。

### R4. 核心三方服务通道打通与登录/支付全闭环
补齐 C 端用户体验所需的外部通道与凭证配置：
- **AI 模型 Provider**：配置正式可用供应商密钥，接入超时重试与错误脱敏回退；
- **登录与认证**：配置真实短信提供商（或在国内未就绪时，智能将“邮箱密码注册/登录”提升为第一默认入口并优雅屏蔽不可用手机号通道，杜绝直接撞墙报错）；
- **支付充值闭环**：配置 Stripe 测试/生产环境凭证，实现套餐购买成功后自动向用户账户增发对应算力额度（`credit_amount`），并将额度充值流水与订单绑定；支持退款反向流水与待对账记录。

### R5. 资产持久化存储与内容风控/法律合规守门
为 C 端用户提供可信赖的数字资产保障与运营合规防护：
- **资产转存**：接入平台自身的对象存储（S3/OSS/持久化存储），对供应商返回的易过期临时结果进行异步转存，确保用户个人作品永不失效、社区展示图不裂；
- **内容安全**：在 Prompt 提交入口与社区发布入口增加敏感词风控拦截，防止违规内容引发上游封号与域名封禁；
- **法律与政策**：将 `/terms`、`/privacy`、`/refund`、`/content-policy` 占位文档替换为包含明确运营主体、AI 免责声明与退款规则的正式合规文本。

## Acceptance Criteria

### 1. 生成与计费闭环验收
- [ ] 前端 ImageStudio 提交生图任务，后端成功扣除额度并在 PostgreSQL 中生成唯一 task 记录与 audit log。
- [ ] 故意制造生成失败或中断，系统 100% 自动返还预扣额度，余额实时恢复，不丢账、不卡账。
- [ ] 刷新页面或在新设备登录后，已生成作品完整出现在 `/creations` 个人资产中心，且可成功播放/下载/一键分享。
- [ ] 零额度账号点击生成时，弹出标准额度不足引导，并可跳转到套餐页面。

### 2. 异步 Worker 稳定性验收
- [ ] Web 进程与 Worker 进程独立运行，`systemctl status go-koyosim-worker` 处于 active (running)。
- [ ] 模拟 kill -9 强杀 Worker 后自动重启，队列中的未决任务自动恢复消费并跑完。
- [ ] 超过预设超时时限的任务被 Watchdog 自动捕获并释放额度，无永久挂起的 `processing` 僵尸任务。

### 3. 数据层与迁移验收
- [ ] 生产数据库完整应用 001~013 全部迁移，`sys_core.schema_migrations` 记录单调递增且 checksum 校验通过。
- [ ] 独立测试库环境全量执行 `npm run test:db`，获得 `6 passed / 0 skipped` 结果。
- [ ] `migration:drill` 与 `backup:restore:drill` 演练成功无阻断。

### 4. 账户与支付闭环验收
- [ ] 匿名用户点击登录，首屏交互流畅，无“短信通道未配置”等破绽弹窗。
- [ ] 邮箱注册新用户可正常收取/验证，新用户自动获得初始体验算力额度（如 10 额度）。
- [ ] Stripe 沙箱测试支付成功后，Webhook 正确验签，用户账户立即获得订阅标示并**自动到账对应算力额度**。
- [ ] 重放相同 Stripe Webhook 事件 ID，系统幂等拦截，绝不重复发放额度。

### 5. 存储、合规与运维验收
- [ ] 用户上传垫图与生成结果保存在平台受控持久存储，图片/视频 URL 长期有效可访问。
- [ ] 输入涉政涉黄违规 Prompt 时，前端拦截并给出友好指引，不透传崩溃。
- [ ] 四大法律页面展示真实运营主体与清晰服务条款。
- [ ] 服务器 Crontab 定时自动备份任务配置就绪并验证可用。
