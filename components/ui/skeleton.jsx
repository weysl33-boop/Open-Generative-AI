import { Skeleton } from "studio/ui/feedback";

/**
 * History kept: the app used to render `animate-pulse bg-muted`, a second
 * loading language next to the studio's shimmer. The shimmer recipe lives in
 * globals.css as `.skeleton`, so this file only re-exports it.
 */
export { Skeleton };
