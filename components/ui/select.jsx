import {
  Select,
  SelectContent,
  SelectGroup,
  SelectItem,
  SelectLabel,
  SelectSeparator,
  SelectTrigger,
  SelectValue,
} from "studio/ui/overlay";

/**
 * History kept: this was a second Select implementation with hard-coded
 * `bg-raised`, a cyan focus ring and a `focus:shadow-elevation-1` glow on
 * every option. The studio primitive owns the recipe now, including the
 * scroll affordances that this file had and the studio copy was missing.
 */
export {
  Select,
  SelectContent,
  SelectGroup,
  SelectItem,
  SelectLabel,
  SelectSeparator,
  SelectTrigger,
  SelectValue,
};
