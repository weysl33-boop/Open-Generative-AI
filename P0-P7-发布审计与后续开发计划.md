# P0-P7 发布审计与后续开发计划

> 文档日期：2026-09-16  
> 项目：Open Generative AI  / KoyoSIM 业务化改造  
> 审计范围：P0–P7 数据层、账户、额度、生成任务、管理后台、Studio、支付、测试、预发布和生产发布准备

## 1. 执行摘要

本次审计的目标不是确认“接口能返回 200”，而是判断应用是否已经具备面向 C 端用户运营的完整条件：数据是否可恢复、额度是否可对账、任务是否可重试、支付是否可验签、权限是否可审计、前端是否能承受真实用户流量，以及发布是否可回滚。

当前结论：

> P0/P1 安全封口与主要 service/repository 边界已完成，静态检查、构建和非数据库业务测试已通过；应用已经具备进入独立 PostgreSQL 16 预发布验收的条件，但还没有达到可以直接面向 C 端正式上线的标准。深度复核确认 `lib/financial/*` 仍是历史财务域 service 内含 SQL 的兼容实现，尚未完全拆成独立 financial Repository；本轮先完成并发锁修复，不把这项残余误报为已完成。

当前最大的未闭环项不是单个页面，而是缺少可重复的 PostgreSQL 16 预发布证据：本轮已用独立 `TEST_DATABASE_URL` 成功跑通一次临时库集成测试，但当前工作机此刻没有 `127.0.0.1:5432` 监听，空库/升级迁移演练、备份恢复和浏览器 E2E 仍不能作为稳定通过项。

### 当前发布判断

| 发布层级 | 当前结论 | 说明 |
| --- | --- | --- |
| 代码静态安全门禁 | 通过 | SQLite runtime、直接旧作品写入、生产 mock、跨域 Cookie 写请求等已处理 |
| 本地构建 | 通过 | Next.js、Studio Babel 和桌面 Vite 构建通过；SettingsModal 已拆为按需 chunk，Vite 仅剩主入口超过 500KB 的性能提示 |
| 单元/静态契约测试 | 通过 | P0、P3、P4、P5、P6、P7、Admin 测试通过 |
| PostgreSQL 集成测试 | 单次通过，待稳定复验 | 已使用独立 `TEST_DATABASE_URL` 创建临时库并通过 6/6；测试结束关闭 pool 并清理临时数据库；需要在稳定 PG16 CI/预发布环境复验 |
| 真实迁移演练 | 未验收 | 当前工作机没有 `127.0.0.1:5432` PostgreSQL 监听；服务器 A 的现有业务库不能作为本次独立演练库 |
| 真实浏览器 E2E | 未验收 | 需要独立测试库和 E2E fixture |
| 真实模型供应商 | 未验收 | 需要预发布 provider 测试凭据 |
| 真实短信登录 | 未验收 | 需要正式 SMS provider 配置和发送验证 |
| Stripe 沙箱支付 | 未验收 | 需要 Stripe 测试账号、价格和 webhook 配置 |
| 正式生产发布 | 不建议 | 仍有 PG16、真实支付/provider/SMS、E2E、法律文本和回滚演练门槛；runtime high/critical 依赖项已清零 |

## 2. 审计范围与方法

本次检查覆盖：

- PostgreSQL-only 数据层和 migration ledger；
- 连接池、超时、参数绑定、事务和健康检查；
- 账户、会话、验证码和管理员 bootstrap；
- 额度预扣、确认、释放、退款和幂等；
- 生成任务状态机、供应商调用和回调；
- 管理后台权限、审计、导出和 Dashboard；
- Studio 路由、懒加载、资源释放和模型目录；
- Stripe、provider、webhook 和错误处理；
- CI、构建、迁移演练、备份恢复和 E2E；
- 生产启动、秘密变量、健康探针和 worker 运行方式。
- 本轮追加验证：DB-owned model catalog、Studio 动态分包/非激活工作台卸载、支付退款反向额度流水、已消费额度待对账义务，以及生成结算/失败释放的原子事务边界。
- 本轮追加安全复核：公开健康/就绪探针只返回最小状态契约，连接池、迁移明细和 provider 诊断保留在管理员健康接口；PG16 临时数据库、迁移演练和备份演练在创建中途失败时也会进入清理路径。
- 本轮追加错误边界复核：API 不再直接返回 `error.message`、错误详情或上游上传响应正文；公开错误通过受限错误码和通用 fallback 脱敏，日志代理参数也先做白名单校验。
- 本轮追加 provider 边界复核：legacy Agent/App/Workflow/Creative Agent/上传代理统一剥离客户端 `Authorization`/`x-api-key`，要求登录；provider 只使用服务端解析的环境变量或加密 PostgreSQL secret。客户端 BYOK 默认关闭，生产只有同时设置 `ALLOW_BYOK=true` 与 `BYOK_SECURITY_REVIEWED=true` 才能进入专项评审后的放行路径。平台会话占位值不会被当作 provider key。
- 本轮追加 Repository 边界收口：Dashboard、用户详情、生成 Worker、额度调账、优惠券、审核、订单退款、支付调用日志和 Stripe Webhook 的 SQL 已下沉到对应 Repository；新增静态测试禁止应用服务重新引入 `query`/`queryOne`/`queryMany`/`execute` 或 `tx.*` 原始 SQL 调用。
- 本轮追加路由边界收口：认证上下文、作品读写、余额、权益、订阅和财务入口均改为显式 Service import；`app` 目录不再直接 import `lib/billing.js`。账户/session 的实际编排已迁入 `lib/services/auth.js`，SQL 已下沉到 `lib/repositories/auth.js`；旧 `lib/billing.js` 现在只保留纯兼容导出，不再包含数据库访问或业务实现。
- 本轮追加认证与密钥安全复核：刷新后的旧 session token 会被 `revoked_at IS NULL` 查询条件拒绝；provider secret 加密不再使用硬编码密钥，优先使用专用 `PROVIDER_SECRETS_ENCRYPTION_KEY`，缺少可用密钥时直接 fail-closed。

验证方式包括：

- 源码搜索和危险路径扫描；
- ESLint、格式和 release safety check；
- Node test 静态/契约测试；
- Next.js 与 Studio 构建；
- 无数据库配置时的启动失败验证；
- 数据库测试入口和环境隔离规则检查；
- 迁移脚本、worker、外部 provider 和 webhook 的代码审查。

## 3. P0 执行结果

### 3.1 PostgreSQL-only 和启动 fail-closed

已完成：

- `DATABASE_URL` 为强制配置；
- 无 `DATABASE_URL` 时应用启动非零退出；
- 无 `DATABASE_URL` 时 generation worker 非零退出；
- 启动脚本不读取 `.env.local` 或 `.env` 作为隐式 fallback；
- PostgreSQL 连接池统一配置连接超时、查询超时、statement timeout、idle timeout 和连接数；
- 查询统一使用 PostgreSQL 原生参数数组绑定；
- 事务统一通过 `withTransaction` 获取连接并负责回滚；
- 增加 pool 关闭能力，worker 退出时可释放连接；
- 健康检查不再将数据库路径当作文件路径处理。

核心文件：

- [lib/db/pg.js](<H:\03 Dev  Lake\lib\db\pg.js>)
- [lib/db/index.js](<H:\03 Dev  Lake\lib\db\index.js>)
- [scripts/require-database-url.mjs](<H:\03 Dev  Lake\scripts\require-database-url.mjs>)

### 3.2 关闭旧的直接作品写入路径

旧的 `POST /api/creations` 可以绕过生成任务状态机与额度账本直接创建成功记录，已关闭为 `410 Gone`。

现在正式生成必须经过：

```text
createGenerationTask
  → reserveCredits
  → queued
  → processing
  → provider call
  → succeeded / failed
  → commit / release
  → generation event + audit log
```

相关文件：

- [app/api/creations/route.js](<H:\03 Dev  Lake\app\api\creations\route.js>)
- [app/api/generations/route.js](<H:\03 Dev  Lake\app\api\generations\route.js>)
- [lib/services/generationCore.js](<H:\03 Dev  Lake\lib\services\generationCore.js>)
- [lib/repositories/creations.js](<H:\03 Dev  Lake\lib\repositories\creations.js>)

### 3.3 Cookie 写接口的请求防护

新增统一 request guard：

- 生产环境校验 `Origin` 或 `Referer`；
- 跨域写请求返回 `403`；
- 超过 body 限制返回 `413`；
- 注册、登录、验证码、资料、登出、支付、优惠券、额度、社区、财务和上传接口已接入；
- 管理员写操作通过统一权限函数继承同源门禁；
- 注册、登录、验证码和生成任务接入 PostgreSQL 持久化限流；
- 限流数据不依赖单进程内存，应用多实例时仍可共享窗口。

文件：

- [lib/security/requestGuard.js](<H:\03 Dev  Lake\lib\security\requestGuard.js:54>)
- [lib/db/migrations/010_security_baseline.sql](<H:\03 Dev  Lake\lib\db\migrations\010_security_baseline.sql>)
- [lib/admin/authz.js](<H:\03 Dev  Lake\lib\admin\authz.js>)

已额外封口的业务写路径：

- 社区发布、点赞、评论、删除、举报；
- 社区“做同款”匿名递增计数；
- K 币兑换、转账、支付密码、签到；
- 上传代理；
- 双 `/api/api/v1` 旧代理。

### 3.4 Mock provider 和短信 fail-closed

模型 mock 仅允许在隔离 E2E 环境显式开启：

```text
ALLOW_MOCK_GENERATION=true
E2E_TEST_MODE=true
```

生产环境禁止使用 mock generation provider。

短信方面已完成：

- `SMS_PROVIDER` 不再默认回退为 mock；
- 生产环境禁止 mock SMS；
- 验证码以 HMAC-SHA256 形式存储；
- 验证码校验使用 timing-safe 比较；
- 验证码发送失败会失效并记录认证审计；
- Twilio 已有真实发送适配器；
- Aliyun/Tencent 当前如果没有启用适配器会明确失败，不会伪造发送成功。

文件：

- [lib/sms.js](<H:\03 Dev  Lake\lib\sms.js:5>)
- [lib/services/generationProviders.js](<H:\03 Dev  Lake\lib\services\generationProviders.js>)

### 3.5 健康检查和就绪检查

新增：

- `GET /api/live`：进程存活探针；
- `GET /api/ready`：数据库和迁移就绪探针；
- `GET /api/health`：公开最小健康摘要；详细数据库、迁移和 provider 诊断仅通过受管理员权限保护的 `/api/admin/health` 提供。

健康检查覆盖：

- PostgreSQL `SELECT 1`；
- 数据库、数据库用户；
- pool total / idle / waiting / max；
- migration pending；
- migration checksum drift；
- provider 配置状态（仅管理员健康接口）。

公开 `/api/health` 和 `/api/ready` 只返回 `status`、数据库/迁移 `ok` 和时间戳，不暴露连接池计数、迁移版本/待执行列表、checksum drift 或 provider 配置详情；完整信息由受 `healthRead` 权限保护的 `/api/admin/health` 提供。

单个依赖检查失败时，系统健康状态会降级，不会让后台健康页整体白屏。

文件：

- [app/api/live/route.js](<H:\03 Dev  Lake\app\api\live\route.js>)
- [app/api/ready/route.js](<H:\03 Dev  Lake\app\api\ready\route.js>)
- [app/api/health/route.js](<H:\03 Dev  Lake\app\api\health\route.js>)
- [lib/services/systemHealth.js](<H:\03 Dev  Lake\lib\services\systemHealth.js>)
- [app/admin/health/page.js](<H:\03 Dev  Lake\app\admin\health\page.js>)

### 3.6 生成任务持久化 worker

已增加独立 worker 入口：

```bash
npm run worker:generation
```

worker 负责：

- 从 PostgreSQL 持久队列读取 `queued` 任务；
- 通过原子 `queued → processing` 更新避免重复执行；
- 调用 provider；
- 成功结算额度；
- 失败释放预扣额度；
- 处理过期额度预扣；
- 处理生成超时；
- 单任务失败后继续处理其他任务；
- 收到终止信号后关闭 PostgreSQL pool。

设置：

```text
GENERATION_ASYNC=true
```

后，生成 API 返回 `202`，任务进入 PostgreSQL 队列，由 worker 消费。worker 未运行时任务会保留在队列中，不会被 HTTP 请求超时或进程重启直接丢失。

文件：

- [scripts/generation-worker.mjs](<H:\03 Dev  Lake\scripts\generation-worker.mjs>)
- [scripts/generation-worker-supervisor.mjs](<H:\03 Dev  Lake\scripts\generation-worker-supervisor.mjs>)
- [lib/services/taskWorker.js](<H:\03 Dev  Lake\lib\services\taskWorker.js:11>)
- [package.json](<H:\03 Dev  Lake\package.json>)

worker 还提供 `worker:generation:supervised` 入口，具备有限重启次数、指数退避、退出信号转发和预算耗尽退出；具体部署方式（systemd、容器编排或平台 supervisor）仍需在预发布环境实测并接入告警。

### 3.7 旧双 API 代理的安全边界

`/api/api/v1/*` 旧代理已调整为：

- 必须登录；
- 不接收客户端传入的 provider key；
- 只使用服务端解析的环境变量或加密 PostgreSQL provider secret；
- 本地 `gen_*` 任务轮询必须校验任务所属用户；
- 其他用户不能通过轮询路径读取任务结果。

同类 legacy provider 代理（Agent、App、Workflow、Creative Agent、上传和 `/api/v1` GET）也已统一要求登录、剥离客户端凭据并在服务端 key 缺失时返回 `503`；`koyosim-account-session` 仅作为平台会话占位标识，不会被当成 provider key 转发。

文件：

- [app/api/api/v1/[[...path]]/route.js](<H:\03 Dev  Lake\app\api\api\v1\[[...path]]\route.js>)

### 3.8 法律页面

已补齐：

- [app/terms/page.js](<H:\03 Dev  Lake\app\terms\page.js>)
- [app/privacy/page.js](<H:\03 Dev  Lake\app\privacy\page.js>)
- [app/refund/page.js](<H:\03 Dev  Lake\app\refund\page.js>)
- [app/content-policy/page.js](<H:\03 Dev  Lake\app\content-policy\page.js>)

当前页面为产品占位版本，必须由运营方和法律顾问审核后替换，不能直接作为正式线上法律文本。

## 4. P0-P7 分阶段审计结论

### P0：上线前安全和运行底座

状态：代码实施完成，真实 PG16 验收未完成。

已完成：

- PostgreSQL-only；
- no-DATABASE_URL fail-closed；
- 旧直接写作品路径关闭；
- request origin/body/rate limit；
- mock/provider fail-closed；
- health/live/ready；
- generation worker 与 bounded-restart supervisor；
- P0 测试和 CI 接入。
- legacy provider 代理登录保护、服务端 key fail-closed 和 BYOK 双开关门禁。

待验收：

- 空 PG16 库迁移；
- 真实 transaction rollback；
- 真实额度 settle/release；
- E2E 三身份；
- worker 重启恢复。

### P1：PostgreSQL-only 数据层与迁移体系

状态：主体代码已完成，主要 service/repository 边界已收口，仍需真实 PostgreSQL 16 数据库实测闭环；旧 `lib/billing.js` 已降级为纯兼容导出层，不再是运行时数据层。

已确认：

- repository/service 分层已建立；
- `queryOne`、`queryMany`、`execute`、`withTransaction` 已替代主要同步访问；
- app/API 页面不再直接 import repository 或 db 模块，查询路径统一经过 service facade，并由 P1 静态边界测试锁定；
- migration ledger 记录版本和 checksum；
- PostgreSQL DDL 覆盖账户、模型、套餐、订单、订阅、额度、生成、Webhook 和审计等域；
- 无 SQLite runtime 依赖。
- 最新迁移已推进到 `013_payment_success_uniqueness.sql`；
- `plans_config` 与 `models_config` 已成为公开套餐、用户权益和模型可用性的数据库来源。
- Dashboard、用户详情、生成 Worker、额度调账、优惠券、审核、订单退款、支付调用日志、Stripe Webhook 和对账查询的原始 SQL 已迁入对应 Repository；主要应用服务不再直接调用原始 `query*`/`execute`。
- 深度复核新增并发账务门禁：充值、转账、K 币兑换、K 币购买会员和额度签到在事务内按锁定快照计算余额；转账双方按稳定顺序一次性锁定，避免并发余额覆盖和互相等待。

剩余风险：

- 必须在全新 PG16 空库执行全部迁移；
- 必须在已有预发布库执行增量迁移；
- 迁移命名仍建议统一为单调、唯一的版本格式，避免相同数字前缀造成运维误读；
- 需要验证迁移失败时事务是否完整回滚；
- 需要验证旧预发布数据结构与最新代码字段兼容。
- `lib/financial/creditService.js`、`currencyService.js`、`exchangeService.js`、`reconciliation.js` 仍包含历史 SQL；它们当前通过 PostgreSQL 参数绑定和事务运行，但尚未满足“所有财务 SQL 只存在 Repository”的最严格架构门槛。发布前应按“先建 financial Repository → service 切换 → 集成回归 → 删除旧直连”完成拆分，不应直接机械搬移。
- `lib/billing.js` 仅保留历史导出的兼容入口；账户/session、订单、权益和作品读取已分别由 `auth`/`billing`/`generations` service 编排，并由对应 Repository 访问 PostgreSQL。后续只需在确认仓库外部消费者已迁移后删除该兼容入口，不能把它当作数据库实测完成的替代证据。

### P2：账户、权限和运营基础

状态：主体功能已有，真实身份和会话场景需 E2E 验收。

重点检查项：

- 注册、登录、刷新、撤销；
- 密码和验证码失败策略；
- OAuth state、callback 和账号绑定冲突；
- 管理员 bootstrap 不能从旧 SQLite 数据推断；
- 管理员角色必须由 PostgreSQL 明确记录；
- session cookie、过期、撤销和异常登录审计。

### P3：账户、额度与生成任务闭环

状态：核心服务和静态测试已有，真实 PG/provider 组合尚未验收。

目标闭环：

```text
注册/登录
  → 会话和权限
  → 模型选择
  → 创建 queued 任务
  → 预扣额度
  → provider
  → 成功扣除或失败释放
  → 作品记录
  → generation event
  → audit log
```

必须实测：

- 重放同一幂等键不会重复预扣；
- provider 成功只结算一次；
- provider 失败只释放一次；
- 重复 callback 不重复入账；
- 超时会释放额度并进入可审计失败状态；
- 用户只能读取自己的作品和任务。

代码侧已补强：

- 任务创建时，额度预扣、生成记录、`credit_reservations.creation_id` 关联、初始 generation event 和审计日志在同一个 PostgreSQL 事务中提交；
- `queued → processing` 的领取、状态事件和审计日志也在同一个事务中提交，领取后若事件或审计写入失败不会留下无事件的 processing 任务；
- 旧 `lib/billing.recordCreation()` 兼容导出已改为 410 废止错误，不能绕过生成服务直接写入作品；
- 邮箱注册、手机号首次创建/绑定、OAuth 主体绑定、会话创建/刷新/撤销及对应 auth event 已收束到 PostgreSQL 事务边界，避免半成品账号和无审计会话；
- provider 成功时，额度 `COMMITTED`、作品进入 `succeeded`、成功事件和审计日志在同一个事务中提交，避免“已扣额度但任务永久 processing”；
- provider 失败、回调失败和 watchdog 超时时，额度释放、任务进入终态、事件和审计日志在同一个事务中提交；事务失败会整体回滚；
- OAuth 已绑定账号、邮箱合并绑定和新主体创建均在同一事务内完成；绑定冲突使用锁和 `RETURNING` 处理并发竞态，不把并发插入静默当成当前用户成功；
- 日常货币/积分对账发现永久额度物化余额不一致时会将报告标记为 `DISCREPANCY_DETECTED`，不会再只记录明细却返回健康状态；
- Stripe 成功账本增加订单级唯一约束（迁移 013），不同事件 ID 的语义重复成功通知不会重复发放额度；
- 退款/支付失败事件在订单尚未同步时保留为可重试状态并返回 5xx，避免乱序 webhook 被错误标记为已处理。

### P4：管理后台与运营能力

状态：权限、审计、导出和 Dashboard 代码测试已有，真实身份 E2E 尚未完成。

覆盖范围：

- 用户、角色、session；
- 模型、provider、密钥状态；
- 套餐、订阅、订单、优惠券；
- 额度人工调整；
- 生成记录、内容审核、Webhook、系统日志；
- 异步导出、敏感字段最小化、过期下载链接和导出审计。

关键要求：

- 未登录返回 `401`；
- 普通用户返回 `403`；
- 管理员按权限矩阵操作；
- 高风险写操作要求幂等键；
- 关键写操作产生审计记录；
- Dashboard 单项查询失败时局部降级。
- 用户封禁、角色调整、会话撤销、用户标签和运营备注的业务写入与审计写入已收束到同一事务；审计落库失败会回滚对应操作，不再静默吞掉审计异常；
- 管理员调额和 provider secret 轮换的账务/密钥写入与审计也已收束到同一事务；
- 套餐、模型、系统设置、优惠券批量创建和内容审核的写入与审计也已对齐事务边界；
- 订单退款的订单状态、支付 ledger、额度反向流水、订阅状态和退款审计在同一数据库事务内提交；上游退款调用仍需通过 provider 幂等键和沙箱对账验证外部副作用补偿；
- 管理员手工重放 Webhook 的重放状态和审计记录在同一事务内提交；
- 系统日志读取接口将日志类型限制为 `out`/`error`，非法类型在解析服务器路径前返回 `422`，避免将查询参数直接带入文件路径；
- legacy Agent/App/Workflow、社区、上传、财务、生成、支付和 callback API 统一使用公开错误脱敏函数，不再把内部异常文本或上游错误正文返回给客户端；
- 生成失败、Webhook 重试状态、备份告警和 worker 日志只保留受限错误码/通用原因，不把 provider 或数据库异常原文写入用户可见字段、操作日志或运维告警正文；

### P5：Studio、Agent 和前端性能

状态：动态分包、激活标签挂载、模型目录门控、资源边界和静态测试已完成，仍需真实浏览器资源指标。

剩余验收：

- 非激活工作台不挂载；
- Studio 工作台使用 direct lazy import，并在标签切换时卸载非激活组件；
- `StudioResourceBoundary` 提供 loading、failure、Error Boundary 和生命周期指标；
- `models_config` 通过 `/api/models/active` 控制可用模型，目录加载失败时模型工作台不会继续使用静态列表；
- 工作台切换不会重复初始化 Konva、Canvas、WebGL 或 timer；
- Workflow 模板请求不再出现无意义 404；
- `/agents`、`/assistant` 导航行为符合设计；
- 旧 `/agents/[id]`、`/agents/[id]/[conversation]`、`/agents/create` 和 `/agents/edit/[id]` 深链只重定向到 Studio，不再在服务端直接读取 provider key cookie 或请求外部 provider；
- 模型列表、生成任务和额度来自统一服务契约；
- 图片、视频、头像、分层、Cinema 等核心路径完成基础 E2E；
- 首屏、切换标签和创建任务网络请求数量可观测。
- 首发语言入口已收敛为英文和简体中文；日文、韩文、繁体中文目前仍是未完成翻译/路由的延期语言，不会在 C 端切换器中展示；全量语言检查可用 `node scripts/validate-i18n.mjs --all --strict` 显式执行。

### P6：支付、Webhook 和外部模型 provider

状态：Provider 抽象、Stripe 签名/幂等/重放、订单级成功唯一性、乱序重试和退款反向额度代码已完成，真实沙箱闭环尚未验收。

优先顺序：

1. Stripe 测试模式；
2. 模型 provider 测试凭据或模拟 provider；
3. Stripe 正式模式；
4. 微信支付、支付宝；
5. 退款、争议、人工对账和异常补偿。

必须满足：

- Webhook 使用原始 body 做签名校验；
- 事件唯一键去重；
- 重复和乱序投递可处理；
- 失败可重试和人工重放；
- 支付事件、订单、订阅、额度账本和审计在事务边界内保持一致；
- provider key 不进入客户端 bundle、日志或 API 响应；
- provider timeout、retry、circuit breaker、调用量和成本可观测。
- 支付退款会追加 `PAYMENT_REFUND` 不可变额度流水；已消费额度无法安全直接扣成负数时，会进入 `credit_refund_obligations` 待对账状态；
- checkout 成功事件遇到已退款订单时不会再次发放额度。
- 同一订单收到不同事件 ID 的成功通知时，订单级支付账本唯一索引和订单级额度幂等键共同阻止重复入账；迁移 013 若发现历史重复成功记录会明确停止，要求先人工对账。
- 退款/支付失败事件找不到订单时会写入可重试状态，并由 webhook API 返回 5xx 触发 Stripe 重试；
- 本地源码已补齐按月 Checkout 额度快照：服务端从已启用套餐读取 `quotaBase + quotaBonus`，在创建订单前校验并写入 `orders.metadata_json`；支付成功不信任客户端或 Stripe metadata 中的积分数，按订单快照发放。旧订单缺少快照时只回落服务端套餐配置，损坏/不一致快照 fail-closed。
- Stripe 新 Checkout 用服务端订单金额构造月付订阅，并在 Subscription metadata 绑定订单、用户和套餐；`checkout.session.completed` / `checkout.session.async_payment_succeeded` 核销首次付款，`invoice.paid` 只接受已实收且金额、币种与基础订单一致的完整 `subscription_cycle`，以发票 ID 建续期订单并幂等记账。出账折扣/税额变动、套餐 proration、缺少平台 metadata、人工 out-of-band 标记付款均不自动发额度。
- Stripe 首次订单和续期订单都保留支付标识；退款前取消对应 Stripe 订阅，退款回调按订单做不可变反向额度流水。Webhook 事件、订单、订阅和额度账本的真实 Stripe 沙箱闭环尚未在本地完成，必须在隔离预发布按第 5 阶段验收后才能启用生产渠道。
- BYOK 当前默认关闭：legacy 代理会拒绝真实客户端 key，登录用户只可走服务端托管 provider key；生产如要开放 BYOK，必须先完成 key 不落库、不进日志/错误响应/构建产物、传输范围、撤销、覆盖和多标签页行为专项评审，再同时设置 `ALLOW_BYOK=true` 与 `BYOK_SECURITY_REVIEWED=true`。

### P7：测试、预发布和生产发布

状态：CI 结构、静态门禁和 worker supervisor 入口已有，真实环境发布演练未完成。

CI 已包含：

- 依赖和锁文件检查；
- 格式和 ESLint；
- Next.js 构建；
- Studio 构建；
- 桌面入口 Vite 构建（`vite:build`）；
- repository/service 测试；
- PostgreSQL 集成测试入口；
- API 权限契约测试；
- 浏览器 E2E 入口；
- migration drill 入口；
- backup/restore drill 入口；
- secret scan；
- dependency audit 入口。

但当前数据库测试因环境缺失被跳过，CI 不能被解释为真实 PG16 已通过。全量 CI 等价回归已加入首发语言校验和桌面 Vite 构建。

## 5. 发布阻断项

以下项目在完成前不建议向 C 端正式开放。

### 阻断项 A：独立 PostgreSQL 16 测试环境

必须准备：

- 独立 PG16 实例；
- 独立数据库，例如 `open_generative_ai_test`；
- 仅供测试使用的连接账号；
- 不与生产数据库共用；
- `TEST_DATABASE_URL` 不得等于 `DATABASE_URL`；
- 测试库可被清理和重建。

### 阻断项 B：真实数据库迁移和事务证据

必须取得：

- 空库全量迁移成功；
- 已有预发布库增量迁移成功；
- migration ledger latest、pending、drifted 正确；
- checksum drift 能阻断启动或发布；
- 迁移失败能回滚；
- 交易、额度、幂等和生成记录满足约束；
- migration drill 和 backup/restore drill 成功。

### 阻断项 C：worker 部署和可恢复性

必须以 supervisor、systemd、容器编排或等效方式运行 worker：

- web 进程和 worker 进程分离；
- worker 异常退出自动重启；
- worker 日志和错误告警可见；
- worker 重启后 queued 任务仍可处理；
- processing 超时可由 watchdog 释放；
- provider 长时间无回调不会永久冻结额度；
- worker 与 web 版本兼容。

### 阻断项 D：真实 provider 和短信配置

必须在预发布完成：

- provider 测试 key 配置；
- provider 成功、失败、超时、重试和回调测试；
- Twilio 或其他真实 SMS provider 配置；
- 验证码发送、过期、错误重试、频率限制测试；
- 日志中不出现 key、token、验证码明文。

### 阻断项 E：Stripe 测试模式支付对账

必须完成：

- checkout；
- success webhook；
- duplicate webhook；
- out-of-order webhook；
- cancel；
- refund；
- order/subscription/ledger/audit 一致性；
- 重放任何 webhook 不得重复发放额度。

### 阻断项 F：法律文本和内容治理

必须完成：

- Terms of Service；
- Privacy Policy；
- Refund Policy；
- Content Policy；
- AI 生成内容免责声明；
- 内容举报、审核、封禁和申诉策略；
- 数据删除、导出和账户注销规则；
- 面向目标地区的隐私、消费者和支付合规审核。

### 阻断项 G：依赖漏洞

本轮已将 Electron 升级到 44.4.1、electron-builder 升级到 26.15.3、Vite 升级到 8.3.0，并刷新 lockfile。当前复核结果为 runtime audit `high=0, critical=0`，全量 `npm audit --audit-level=moderate` 也为 0 vulnerabilities；此前设计代理工作区残留的旧 PrismJS/refractor 锁定链已清理。

上线前必须：

- 逐项判断是否影响生产 runtime；
- 已升级 Next、Axios、PostCSS、Sharp，并更新 lockfile；
- 继续在依赖升级时审查设计 Agent 的 `react-syntax-highlighter`/`refractor` 锁定链；当前锁文件已解析到安全版本；
- 对未来暂时无法升级的漏洞记录风险接受、影响范围和缓解措施；
- 将实际影响生产路径的 critical/high 漏洞设为发布阻断；
- 重新执行 audit 和 lockfile 审查；当前复核命令已确认 high/critical 均为 0。

### 阻断项 H：BYOK 和 legacy 代理边界复核

平台 provider secret 已保持服务端配置；legacy Agent/App/Workflow/Creative Agent/上传代理现已默认拒绝用户自带 key，并统一要求登录。若首发要重新开放 BYOK，必须在预发布验证：

- key 不落库、不进入普通日志、不进入错误响应和客户端构建产物；
- 只向用户明确选择的上游发送，并在请求结束后不继续保留；
- cookie/header 传输、撤销、覆盖和多标签页行为可解释；
- 若无法完成专项评审，首发继续保持 `ALLOW_BYOK=false`，而不是默认放开。

### 阻断项 I：财务域 Repository 完整收口

本轮已经修复充值、转账、兑换、会员购买和签到的事务锁定，并将对账查询迁入 `lib/repositories/reconciliation.js`；但 `lib/financial/creditService.js`、`currencyService.js`、`exchangeService.js` 仍保留历史 SQL。它们暂时可以在 PostgreSQL 上运行，但未达到 P1 规定的“Repository 承担全部数据读写、service 只做业务编排”的严格标准。

发布前必须：

- 建立 financial Repository 的小接口，封装钱包、流水、预扣、结算、释放、退款和人工调账读写；
- service 不再 import `../db/index.js`，也不再出现 SQL 文本；
- 通过真实 PG16 并发、回滚、幂等和余额对账测试；
- 确认新 Repository 与生成、账务、退款、Webhook、后台调额调用方的结果契约完全一致后，删除旧直连实现。

## 6. 后续开发计划

### 阶段 1：PG16 测试环境和迁移验收

目标：拿到所有数据层和事务逻辑的真实数据库证据。

任务：

1. 创建独立 PostgreSQL 16 测试实例和数据库；
2. 配置只在当前测试环境可用的 `TEST_DATABASE_URL`；`test:db` 每次运行都会从该连接创建临时数据库，避免共享测试数据；
3. 执行全量迁移；
4. 检查 migration ledger、checksum、pending、drifted；
5. 执行 repository 数据库测试；
6. 执行 transaction rollback 测试；
7. 执行幂等唯一约束测试；
8. 执行额度预扣、成功结算、失败释放测试；
9. 执行空库和已有预发布结构迁移演练；
10. 执行备份、恢复和应用回滚演练；
11. 建立 `lib/repositories/financial.js` 的深模块接口，先覆盖钱包、货币流水、额度钱包、预扣凭单和对账查询，再将 `lib/financial/*` 的 SQL 逐步迁入；每一批迁移后跑真实 PG16 回归，确认后删除 service 内直连 SQL。

验收标准：

```text
db:migrate:test       成功
test:db               6 passed / 0 skipped
migration:drill       成功
backup/restore drill  成功
financial repository   无 service 内直连 SQL，事务/并发回归通过
```

执行入口：

```powershell
$env:TEST_DATABASE_URL = "postgresql://user:password@host:5432/open_generative_ai_test"
Remove-Item Env:DATABASE_URL -ErrorAction SilentlyContinue

npm run db:migrate:test
npm run test:db
npm run migration:drill
npm run backup:restore:drill
```

注意：不要将含真实密码的连接字符串提交到 Git，也不要把它发送到聊天记录。

### 阶段 2：P3 业务闭环和 worker 验收

目标：验证 C 端用户一次生成从登录到结果回流的完整链路。

任务：

1. 使用测试 provider 或 mock provider 创建 queued 任务；
2. 启动独立 generation worker；
3. 验证 queued → processing → succeeded；
4. 验证 queued → processing → failed；
5. 验证成功只 commit 一次；
6. 验证失败只 release 一次；
7. 重放相同 idempotency key；
8. 重放供应商 callback；
9. 模拟 worker 重启；
10. 模拟 provider 超时；
11. 检查任务、额度、generation events、provider call logs 和 audit logs 是否关联。

验收标准：

- 任意请求重放不会重复扣额；
- 任意回调重放不会重复入账；
- 用户可以看到余额、明细、任务历史和失败原因；
- 管理员可以从任务追踪到用户、模型、provider、额度和审计事件；
- worker 重启不会丢失 queued 任务。

### 阶段 3：账户、安全和权限 E2E

目标：用真实浏览器确认匿名、普通用户和管理员边界。

任务：

1. 注册；
2. 登录；
3. 修改资料；
4. 刷新会话；
5. 注销和撤销会话；
6. 失败密码和验证码重试；
7. 账号绑定冲突；
8. 匿名访问后台；
9. 普通用户访问后台；
10. 管理员访问 Dashboard；
11. 管理员调整额度并验证审计；
12. 验证管理员关键写操作的幂等重放。

验收标准：

| 身份 | 普通用户页面 | 管理后台页面 | 管理 API |
| --- | --- | --- | --- |
| 匿名 | 只能访问公开页面 | 不可访问 | `401` |
| 普通用户 | 可访问自己的账户和作品 | 不可访问 | `403` |
| 管理员 | 可按角色访问 | 按权限矩阵访问 | `200` 或明确业务拒绝 |

### 阶段 4：Studio、Agent 和前端资源验收

目标：确认真实浏览器不会隐式加载所有工作台，也不会重复初始化高资源组件。

任务：

1. 分别打开图片、视频、头像、分层、Cinema、Workflow、Agent；
2. 检查非激活标签是否挂载；
3. 切换标签并观察网络请求；
4. 检查 Konva、Canvas、WebGL、timer 和 event listener 清理；
5. 观察首屏 JS、切换耗时和资源峰值；
6. 创建生成任务并观察结果回流；
7. 验证失败重试和额度展示；
8. 验证模型目录由 API/Repository 提供，不依赖过期静态列表；
9. 验证 Workflow 模板无隐藏 404；
10. 验证 `/agents` 和 `/assistant` 路由。

验收标准：

- 控制台无重复 Konva 实例；
- 控制台无隐藏 Workflow 模板 404；
- 非激活 Studio 不产生无关 provider 请求；
- 任务、额度、失败和结果回流契约一致；
- 高资源工作台具备 loading、empty、error 和 Error Boundary。

### 阶段 5：Stripe 沙箱和 provider 对账

目标：在不接入正式支付前，完成可重放、可对账的沙箱闭环。

任务：

1. 配置 Stripe test secret；
2. 配置 test price；
3. 配置 test webhook secret；
4. 完成支付成功流程；
5. 重放同一个事件；
6. 乱序投递订单和订阅事件；
7. 测试取消；
8. 测试退款；
9. 验证订单状态、订阅状态、支付 ledger、credit ledger 和 audit log；
10. 验证 provider 成功、失败、超时、retry、熔断和调用成本记录。
11. 明确 Pro/Team 是否包含算力额度；若包含，先在套餐目录增加额度 entitlement，再验证首次支付、续期、退款、取消和乱序 webhook 的额度账本结果；若不包含，验收中必须明确“订阅只提供功能权限，不发放额度”。

验收标准：

- 任何 webhook 重放都不会重复发放额度；
- 支付成功不能只修改余额字段；
- 退款会留下可追溯的反向账务记录；
- 退款不会把用户余额扣成负数；已消费的订单额度会形成待对账义务；
- 所有事件都有事件 ID、订单关联和审计记录；
- 失败事件可重试，重试不会破坏状态机。
- 套餐权益与额度契约已书面确定，前端展示、订单、订阅和额度账本一致。

### 阶段 6：运营、导出和内容治理

目标：让后台能够安全处理真实运营事务。

任务：

1. 用户审核；
2. 角色管理；
3. session 管理；
4. 模型/provider 状态管理；
5. 套餐、订阅、订单和优惠券；
6. 额度人工调整；
7. 生成记录和失败任务；
8. 内容举报和审核；
9. Webhook 重放；
10. 系统日志；
11. 异步导出；
12. 下载权限、过期和敏感字段最小化；
13. 导出审计。

验收标准：

- 每个高风险写接口有权限、输入校验、幂等和审计；
- Dashboard 单项查询失败不造成整体白屏；
- 导出任务不会同步占用 HTTP 请求；
- 导出内容不含不必要的 secret、password、token 或完整敏感字段；
- 管理员可以通过审计日志重建一次关键操作。

### 阶段 7：生产发布演练和灰度上线

目标：验证生产发布可观测、可回滚、可恢复。

任务：

1. 生产发布前创建 PostgreSQL 备份；
2. 记录当前 schema/migration 版本；
3. 执行 forward-compatible migration；
4. 发布应用和 worker；
5. 保持旧应用可回滚；
6. 检查 `/api/live` 和 `/api/ready`；
7. 观察登录错误率；
8. 观察生成失败率；
9. 观察额度异常；
10. 观察支付 webhook 失败；
11. 观察连接池 total/idle/waiting；
12. 观察接口 `5xx`；
13. 执行小流量灰度或维护窗口发布；
14. 执行回滚演练；
15. 确认备份恢复路径。

正式支付启用、生产密钥替换、生产迁移、生产 worker 切换和正式发布都属于外部实际变更，应在执行前单独确认。

## 7. 推荐的发布门禁

### 必须全部满足

- [ ] `DATABASE_URL` 指向 PostgreSQL 16；
- [ ] `TEST_DATABASE_URL` 指向独立 PostgreSQL 16 测试库；
- [ ] `test:db` 为 6 passed / 0 skipped；
- [ ] 空库迁移成功；
- [ ] 预发布增量迁移成功；
- [ ] migration checksum 无 drift；
- [ ] backup/restore drill 成功；
- [ ] 生成成功结算通过；
- [ ] 生成失败释放通过；
- [ ] 重复请求不重复扣额；
- [ ] 重复 webhook 不重复入账；
- [ ] worker 可监控、可重启、可恢复；
- [ ] 未登录、普通用户、管理员 E2E 通过；
- [ ] 真实 provider 测试通过；
- [ ] 真实 SMS 测试通过；
- [ ] Stripe test mode 对账通过；
- [ ] 法律文本完成审核；
- [ ] runtime critical/high 依赖漏洞为 0；完整依赖树的开发期 critical/high 已升级或获明确风险接受；
- [ ] 生产备份和回滚路径已演练；
- [ ] `/api/live`、`/api/ready`、日志和告警可用。

### 任何一项不满足时

只能进行：

- 内部开发测试；
- 独立预发布测试；
- 受控邀请用户测试；
- 不涉及真实付款、真实生产额度和生产敏感数据的演示。

不应进行：

- 面向不特定 C 端用户的正式开放；
- 正式支付开通；
- 生产 provider key 切换；
- 不可回滚的数据库结构删除；
- 未备份状态下的生产迁移。

## 8. 当前下一步

当前最直接的下一步是把本轮已验证过的独立 `TEST_DATABASE_URL` 固化到稳定的 PostgreSQL 16 CI/预发布环境，并补齐尚未形成稳定证据的迁移演练、备份恢复和浏览器 E2E，然后按阶段 1/2 执行：

```text
db:migrate:test
→ test:db
→ migration:drill
→ backup:restore:drill
→ e2e:seed
→ test:e2e
→ e2e:clean
```

在这组命令全部成功之前，数据库层的可重复发布证据和真实 C 端业务闭环仍属于“代码已准备、环境未验收”状态。

本次审计和 P0 实施没有操作生产数据库、生产支付、生产短信或生产模型供应商，也没有清理工作区中原有的用户改动和生成文件。

## 9. 2026-09-16 本轮复核证据

代码侧 CI 等价回归已完成，结果如下：

| 检查 | 结果 | 说明 |
| --- | --- | --- |
| `npm run test:ci` | 未形成稳定完整结果 | 已通过到 `test:db` 前的全部静态/契约门禁；一次完整运行在临时 PG 连接阶段长时间等待后被安全终止，不能把它标记为 CI 全绿；独立 `npm run test:db` 曾通过 6/6 |
| `npm run format:check` | 通过 | 格式策略通过 |
| `npm run lint` | 通过 | `--max-warnings=0` |
| `npm run release:check` | 通过 | 474 个源码文件通过 PostgreSQL-only、硬编码数据库兜底和禁用运行时检查 |
| `npm run security:secrets` | 通过 | 测试 fixture 和环境模板按规则排除 |
| `npm run test:i18n` | 通过 | 首发语言 en/zh-CN；zh-CN 有 18 个英文 fallback |
| `npm run test:p0` | 17/17 通过 | 安全底座、fail-closed、健康端点最小化、app/API service 边界、服务/Repository 边界、API 错误脱敏，以及 BYOK/legacy provider 代理边界 |
| `npm run test:admin` | 16/16 通过 | 管理员权限、异步边界、Dashboard 契约，以及 P2 账户/OAuth/动态 provider 配置契约 |
| `npm run test:p3` | 7/7 通过 | 生成状态机/provider mock/结算原子事务/账号事务/旧直写旁路封口；OAuth 并发保护、对账 Repository、对账状态修复和财务钱包锁定快照已通过 lint/格式策略复核 |
| `npm run test:p4` | 10/10 通过 | 后台权限、导出、Dashboard、幂等、供应商健康检查和系统日志路径参数校验 |
| `npm run test:p5` | 6/6 通过 | Studio 按需加载、模型目录、资源清理 |
| `npm run test:p6` | 9/9 通过 | Stripe/provider/退款账本/乱序重试结构，以及服务端 provider secret 解析边界 |
| `npm run test:p7` | 8/8 通过 | CI、临时数据库隔离、迁移演练、E2E/备份门槛契约、worker supervisor、测试库目标保护和预构建产物回滚门禁；临时资源异常清理已复核 |
| `npm run test:db` | 单次 6/6 通过；当前环境不可复验 | 已用独立 `TEST_DATABASE_URL` 完成全量迁移、参数绑定、事务回滚、幂等、生成失败释放和退款反向账本；测试池已显式关闭并清理临时库；随后演练时本机 `127.0.0.1:5432` 无监听 |
| `npm run migration:drill` | 当前环境未完成 | 脚本已拒绝在无 PostgreSQL 监听的 `127.0.0.1:5432` 上继续；代码包含空库全量迁移、008→009+ 增量迁移和 `DROP COLUMN` 前向兼容检查 |
| `npm run backup:restore:drill` | 当前环境未完成 | 脚本已拒绝在无 PostgreSQL 监听的 `127.0.0.1:5432` 上继续；代码包含 custom dump、独立恢复库、marker 校验和 `finally` 清理 |
| `npm run build:studio` | 通过 | Babel 编译 68 个文件；models.js 有大文件提示 |
| `npm run vite:build` | 通过 | 生成桌面 Vite 分包；SettingsModal 已改为纯按需 chunk，仍有主入口超过 500KB 的性能提示 |
| `npm run build` | 通过 | Next.js 15.5.25，68 个静态页面生成；Studio 路由首载约 160 kB（动态工作台按需加载） |
| `npm audit --omit=dev` | 0 vulnerabilities | runtime high/critical 为 0 |
| `npm audit --audit-level=moderate` | 0 vulnerabilities | 设计代理旧 PrismJS/refractor 锁定链已清理 |
| 导出后台 worker | 通过静态契约 | 导出状态查询不再同步处理；独立 worker claim/处理 queued job，systemd 失败自动重启 |
| `npm run test:e2e` | 未通过，按预期 fail-closed | 没有 `DATABASE_URL` 时 webServer 启动退出 1；需在隔离 PG16 库完成 seed 后复验 |
| E2E 数据库绑定 | 通过静态契约 | Playwright webServer 强制使用 `TEST_DATABASE_URL`，拒绝继承应用库或缺少隔离测试库 |
| 生产部署脚本 | 通过静态契约 | `deploy-safe.mjs` 只接受带 `BUILD_ID` 的预构建产物，原子切换并保留 `.next-previous`，健康检查失败自动回滚 |
| `node scripts/validate-i18n.mjs --all --strict` | 未通过 | 日/韩/繁中仍为延期语言，缺少约 934 个键且未开放入口 |

补充的只读环境证据：服务器 A（`43.155.166.90`）可见 PostgreSQL `16.13`，但当前没有可供本机稳定复用的独立预发布数据库；现有 `koyosim_ai` 数据库已有用户、会话和业务数据，migration ledger 只到 `006_community_and_social`。因此该库不能直接作为本次阶段 1 的测试库，也没有对它执行迁移、测试、备份或写入；已在本机独立 `TEST_DATABASE_URL` 上完成一次临时库集成测试，但必须在明确的 PG16 CI/预发布数据库上重新完成迁移、备份恢复和 E2E。

结论：源码和构建门槛已经收敛到可交付独立预发布的状态；真实 PostgreSQL 16、worker supervisor 部署与告警、短信/provider/Stripe 沙箱、浏览器 E2E、备份恢复、法律审核仍是上线前的外部验收条件。未取得这些证据前，不得面向不特定 C 端用户正式开放。
