# E2E Test Infra: Open Generative AI (KoyoSIM AI Studio)

## 1. 测试理念与架构原则 (Test Philosophy)
- **Opaque-box (黑盒契约驱动)**：测试用例完全基于 `ORIGINAL_REQUEST.md` 与对外 API/UI 契约设计，不依赖内部私有函数或底层具体实现。
- **4-Tier 系统化分层覆盖**：
  - **Tier 1 (功能基线覆盖)**：验证每个原子特性（F01~F19）的标准正常路径（Happy-path），≥5 个独立用例/特性。
  - **Tier 2 (边界与异常防御)**：覆盖输入极端值、网络闪断、强杀中断、零额度、超时熔断、敏感词违规等异常分支，≥5 个独立用例/特性。
  - **Tier 3 (跨功能组合交互)**：验证特性间组合交互（如：邮箱注册赠额 -> 零额度拦截 -> Stripe 充值到账 -> 生成扣减 -> 异常退款 -> 作品持久化），覆盖主要交互路径。
  - **Tier 4 (真实业务端到端应用场景)**：真实端到端场景流（创作者日常：登录 -> 垫图创作 -> 异步排队 -> 超清转存 -> 一键做同款与社区发布）。

---

## 2. 特性清单与测试覆盖矩阵 (Feature Checklist)

| # | Feature 代号与名称 | Tier 1 (基线覆盖) | Tier 2 (边界异常) | Tier 3 (组合交互) | Tier 4 (端到端场景) |
|---|---|:---:|:---:|:---:|:---:|
| F01 | PG 迁移基石推进 (007~013) | 5 用例 | 5 用例 | ✓ | ✓ |
| F02 | 独立测试库运行 (test:db) | 5 用例 | 5 用例 | ✓ | - |
| F03 | 迁移与备份恢复演练 | 5 用例 | 5 用例 | - | ✓ |
| F04 | Worker 生产常驻守护 | 5 用例 | 5 用例 | ✓ | ✓ |
| F05 | 异步任务生成剥离 (ASYNC) | 5 用例 | 5 用例 | ✓ | ✓ |
| F06 | Worker 孤儿自愈与断点续跑 | 5 用例 | 5 用例 | ✓ | ✓ |
| F07 | Watchdog 超时熔断与自愈 | 5 用例 | 5 用例 | ✓ | ✓ |
| F08 | Studio 工作台统一契约接入 | 5 用例 | 5 用例 | ✓ | ✓ |
| F09 | 额度原子预扣与释放对账 | 5 用例 | 5 用例 | ✓ | ✓ |
| F10 | 作品沉淀与做同款 Remix | 5 用例 | 5 用例 | ✓ | ✓ |
| F11 | 零额度引导与充值弹窗 | 5 用例 | 5 用例 | ✓ | ✓ |
| F12 | 邮箱优先与认证降级 | 5 用例 | 5 用例 | ✓ | ✓ |
| F13 | 新用户赠送初始体验额度 | 5 用例 | 5 用例 | ✓ | ✓ |
| F14 | Stripe 支付自动发放额度 | 5 用例 | 5 用例 | ✓ | ✓ |
| F15 | Webhook 事件幂等与退款流水 | 5 用例 | 5 用例 | ✓ | - |
| F16 | 资产持久化转存管道 | 5 用例 | 5 用例 | ✓ | ✓ |
| F17 | Prompt 与社区敏感词拦截 | 5 用例 | 5 用例 | ✓ | ✓ |
| F18 | 四大合规政策文本填充 | 5 用例 | 5 用例 | - | ✓ |
| F19 | 生产定时自动备份就绪 | 5 用例 | 5 用例 | - | ✓ |

---

## 3. 测试架构与运行环境 (Test Architecture)

### 3.1 测试套件目录布局
```
e2e-tests/
├── runner.mjs                 # 顶层测试驱动器（支持按 Tier 运行与退出码管理）
├── helpers/
│   ├── api-client.mjs         # 统一 HTTP API 客户端封装
│   ├── db-verifier.mjs        # 数据库状态直连校验器 (PG Client)
│   └── mock-provider.mjs      # 仿真 AI 模型与 Stripe Webhook 签名生成器
├── tier1-features/            # Tier 1 针对各 Feature 的基线测试
├── tier2-boundaries/          # Tier 2 边界极限与异常熔断测试
├── tier3-combinations/        # Tier 3 跨模块数据流与计费生命周期组合测试
└── tier4-scenarios/           # Tier 4 真实商业全场景流水线
```

### 3.2 运行与判定语义 (Pass/Fail Semantics)
- **统一执行命令**：`node e2e-tests/runner.mjs --tier=all`
- **判定语义**：全部测试通过退出码为 0，任一测试失败退出码非 0，测试日志输出详细断言与耗时，供 Reviewer 与 Forensic Auditor 进行审计。

---

## 4. 真实业务端到端应用场景 (Tier 4 Scenarios)

1. **场景 1: 新创作者注册到首次作品沉淀**
   - 邮箱注册 -> 自动到账 10 额度 -> 访问 ImageStudio 提交生图 -> 异步入队并由 Worker 消费 -> 生成完成自动转存持久化 S3 -> 作品在 `/creations` 正常显示并支持高清下载。
2. **场景 2: 额度耗尽与 Stripe 沙箱购买充值闭环**
   - 连续消耗额度至 0 -> 再次提交被 402 拦截并弹出 RechargeModal -> 发起 Stripe Checkout -> 触发 Webhook 模拟充值 -> 账户自动到账 500 额度 -> 重启创作成功扣费。
3. **场景 3: 服务突发故障与超时自愈 (Watchdog & Kill-9)**
   - 提交耗时视频任务 -> 预扣 5 额度 -> 强杀 Worker 进程 (`kill -9`) -> Worker 重启自愈恢复未决任务 -> 若上游模型无响应超过超时阈值 -> Watchdog 熔断释放 5 额度 -> 账户额度实时恢复。
4. **场景 4: 社区作品同款 (Remix) 链条**
   - 用户 A 发布作品至社区 -> 用户 B 浏览社区点击“做同款” -> 工作台自动带入提示词与参数 -> 用户 B 调整后扣额度生成专属新作品。
5. **场景 5: 安全风控与合规审查**
   - 提交包含违规敏感词的 Prompt -> 立即返回 400 并提示友好合规指引 -> 额度不预扣 -> 检查四大合规页面法律主体与服务政策。
