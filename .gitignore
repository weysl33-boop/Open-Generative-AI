# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Electron build output
release/
.webpack/

# Next.js
.next/
.next-*/
out/

# Local runtime/session data and generated archives
cookies.txt
data/backups/
*.db
*.tar
*.tar.gz

# Agent scratch workspace: may contain host credentials and must stay local
.agents/

# Misc
*.pem
.env
.env.*
!.env.example

# Temporary P7 browser fixtures
e2e/.sessions.json
test-results/

# Runtime IP-block state written by the app
lib/security/ip_block_state.json
# 运行时镜像绝不能进版本库：带着 enabled:true 被部署出去等于直接锁死境内全部访问
data/ip_block_state.json
data/china_ip_ranges.json

# Local release snapshots and merchant key material must never be committed.
release-staging/
支付宝密钥/
.qoder/
nav-audit-results/
audit_summary_generated.json
frontend_audit_issues.json
chunks.progress
test_ping.txt

# UI drift audit baseline, per-run reports and evidence screenshots
.ui-drift/
audit_screenshots/
runtime_ui_audit_result.json
source_ui_audit_result.json

# 快照上线法的本地暂存区（拉线上源码 + 只覆盖自己那一笔的构建沙箱）
.ship-*/
.split/
.srctar/
.head-probe/
